--- a/src/circllhist.h
+++ b/src/circllhist.h
@@ -158,6 +158,17 @@
 API_EXPORT(int) hist_bucket_idx_bucket(const histogram_t *hist, int idx, hist_bucket_t *b, uint64_t *c);
 //! Accumulate bins from each of cnt histograms in src onto tgt
 API_EXPORT(int) hist_accumulate(histogram_t *tgt, const histogram_t * const *src, int cnt);
+//! Count samples below each sorted threshold in one histogram traversal.
+API_EXPORT(void) hist_approx_count_below_many(const histogram_t *hist, const double *thresholds,
+                                              int count, uint64_t *results);
+//! Compute sum, sample count, and bucket counts in one histogram traversal.
+API_EXPORT(void) hist_approx_stats(const histogram_t *hist, const double *thresholds, int count,
+                                   uint64_t *below, uint64_t *sample_count, double *sample_sum,
+                                   uint64_t *above);
+//! Accumulate source bins into tgt by inserting each source bucket in place.
+//! The target must not alias any source histogram.
+API_EXPORT(int) hist_accumulate_by_insert(histogram_t *tgt,
+                                          const histogram_t * const *src, int cnt);
 //! Subtract bins from each of cnt histograms in src from tgt, return -1 on underrun error
 API_EXPORT(int) hist_subtract(histogram_t *tgt, const histogram_t * const *src, int cnt);
 //! Subtract bins in src from tgt treating the result count as signed, return -1 on overflow error
@@ -166,6 +170,6 @@
 API_EXPORT(int) hist_add_as_int64(histogram_t *tgt, const histogram_t *src);
 //! Clear data fast. Keeps buckets allocated.
 API_EXPORT(void) hist_clear(histogram_t *hist);
+//! Clear data from each of count histograms. Keeps buckets allocated.
+API_EXPORT(void) hist_clear_many(histogram_t * const *histograms, int count);
 //! Insert a value into a histogram value = val * 10^(scale)
 API_EXPORT(uint64_t) hist_insert_intscale(histogram_t *hist, int64_t val, int scale, uint64_t count);
 
